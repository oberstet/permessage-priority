# Per-message Priority Extension

This repository contains an RFC draft for `permessage-priority`, an extension to WebSocket as specified in RFC6455 for priority-based transmission of WebSocket messages.
